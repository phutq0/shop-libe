const className = {
    wrapper: `flex w-full md:flex-col md:w-60`,
    item: `
        flex-1 md:flex-[0] py-2 text-sm border flex items-center justify-center 
        cursor-pointer text-gray-500`,
    active: `
        flex-1 md:flex-[0] py-2 text-sm border-2 flex items-center justify-center 
        cursor-pointer text-white bg-black font-semibold border-gray-400
    `
}

export default className